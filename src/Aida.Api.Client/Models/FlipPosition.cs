// <auto-generated/>
#nullable enable
using System.Text.Json.Serialization;

namespace Aida.Api.Client {
    [JsonConverter(typeof(FlipPositionJsonConverter))]
    public enum FlipPosition {
        Front,
        Rear,
        MagneticEncoder,
        VerticalEncoder,
    }

    internal static class FlipPositionExtensions {
        public static string ToWireValue(this FlipPosition value) {
            return value switch
            {
                FlipPosition.Front => "Front",
                FlipPosition.Rear => "Rear",
                FlipPosition.MagneticEncoder => "MagneticEncoder",
                FlipPosition.VerticalEncoder => "VerticalEncoder",
                _ => value.ToString(),
            };
        }

        public static FlipPosition ParseFlipPosition(this string value) {
            return value switch
            {
                "Front" => FlipPosition.Front,
                "Rear" => FlipPosition.Rear,
                "MagneticEncoder" => FlipPosition.MagneticEncoder,
                "VerticalEncoder" => FlipPosition.VerticalEncoder,
                _ => throw new System.ArgumentException($"Unknown FlipPosition value: {value}"),
            };
        }
    }

    internal sealed class FlipPositionJsonConverter : System.Text.Json.Serialization.JsonConverter<FlipPosition> {
        public override FlipPosition Read(ref System.Text.Json.Utf8JsonReader reader, System.Type typeToConvert, System.Text.Json.JsonSerializerOptions options) {
            var s = reader.GetString() ?? string.Empty;
            return s.ParseFlipPosition();
        }
        public override void Write(System.Text.Json.Utf8JsonWriter writer, FlipPosition value, System.Text.Json.JsonSerializerOptions options) {
            writer.WriteStringValue(value.ToWireValue());
        }
    }
}
