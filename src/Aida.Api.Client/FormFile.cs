// <auto-generated/>
#nullable enable
using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Aida.Api.Client
{
    /// <summary>
    /// Binary content with an optional file name for multipart uploads.
    /// Implicitly convertible from <c>byte[]</c> for convenience.
    /// </summary>
    [JsonConverter(typeof(FormFileJsonConverter))]
    public readonly struct FormFile
    {
        public byte[] Content { get; }
        public string? FileName { get; }

        public FormFile(byte[] content, string? fileName = null)
        {
            Content = content ?? throw new ArgumentNullException(nameof(content));
            FileName = fileName;
        }

        public static implicit operator FormFile(byte[] content) => new FormFile(content);
    }

    internal sealed class FormFileJsonConverter : JsonConverter<FormFile>
    {
        public override FormFile Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
        {
            var bytes = reader.GetBytesFromBase64();
            return new FormFile(bytes);
        }

        public override void Write(Utf8JsonWriter writer, FormFile value, JsonSerializerOptions options)
        {
            writer.WriteBase64StringValue(value.Content);
        }
    }
}
