// <auto-generated/>
#nullable enable
using System;
using System.IO;

namespace Aida.Api.Client.Responses {
    /// <summary>
    /// Discriminated response for GetVacuumPressure. Exactly one of the typed properties corresponding to {Status} will be non-null; the others will be null. Use {Match} for exhaustive handling.
    /// </summary>
    public sealed class GetVacuumPressureResult {
        public int Status { get; }
        public string? RawBody { get; }

        public float? Ok { get; }

        public bool IsOk => Status == 200;
        public bool IsSuccessStatus => Status >= 200 && Status < 300;

        private GetVacuumPressureResult(int status, string? rawBody, float? ok) {
            Status = status;
            RawBody = rawBody;
            Ok = ok;
        }

        internal static GetVacuumPressureResult FromOk(float? value, string? rawBody = null) {
            return new GetVacuumPressureResult(200, rawBody, value);
        }

        internal static GetVacuumPressureResult FromOther(int status, string? rawBody = null) {
            return new GetVacuumPressureResult(status, rawBody, default);
        }

        /// <summary>
        /// Exhaustive match across all declared response variants. The onOther handler covers any status not declared in the OpenAPI spec.
        /// </summary>
        public T Match<T>(Func<float, T> onOk, Func<int, string?, T> onOther)
        {
            if (Status == 200) return onOk(Ok!.Value);
            return onOther(Status, RawBody);
        }
    }
}
