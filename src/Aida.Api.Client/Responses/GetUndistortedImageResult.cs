// <auto-generated/>
#nullable enable
using System;
using System.IO;

namespace Aida.Api.Client.Responses {
    /// <summary>
    /// Discriminated response for GetUndistortedImage. Exactly one of the typed properties corresponding to {Status} will be non-null; the others will be null. Use {Match} for exhaustive handling.
    /// </summary>
    public sealed class GetUndistortedImageResult {
        public int Status { get; }
        public string? RawBody { get; }

        public global::System.IO.Stream? Ok { get; }
        public global::Aida.Api.Client.NotFoundResult? NotFound { get; }

        public bool IsOk => Status == 200;
        public bool IsNotFound => Status == 404;
        public bool IsSuccessStatus => Status >= 200 && Status < 300;

        private GetUndistortedImageResult(int status, string? rawBody, global::System.IO.Stream? ok, global::Aida.Api.Client.NotFoundResult? notFound) {
            Status = status;
            RawBody = rawBody;
            Ok = ok;
            NotFound = notFound;
        }

        internal static GetUndistortedImageResult FromOk(global::System.IO.Stream? value, string? rawBody = null) {
            return new GetUndistortedImageResult(200, rawBody, value, default);
        }

        internal static GetUndistortedImageResult FromNotFound(global::Aida.Api.Client.NotFoundResult? value, string? rawBody = null) {
            return new GetUndistortedImageResult(404, rawBody, default, value);
        }

        internal static GetUndistortedImageResult FromOther(int status, string? rawBody = null) {
            return new GetUndistortedImageResult(status, rawBody, default, default);
        }

        /// <summary>
        /// Exhaustive match across all declared response variants. The onOther handler covers any status not declared in the OpenAPI spec.
        /// </summary>
        public T Match<T>(Func<global::System.IO.Stream, T> onOk, Func<global::Aida.Api.Client.NotFoundResult, T> onNotFound, Func<int, string?, T> onOther)
        {
            if (Status == 200) return onOk(Ok!);
            if (Status == 404) return onNotFound(NotFound!);
            return onOther(Status, RawBody);
        }
    }
}
