// <auto-generated/>
#nullable enable
using System;
using System.IO;

namespace Aida.Api.Client.Responses {
    /// <summary>
    /// Discriminated response for EnableDebugMode. Exactly one of the typed properties corresponding to {Status} will be non-null; the others will be null. Use {Match} for exhaustive handling.
    /// </summary>
    public sealed class EnableDebugModeResult {
        public int Status { get; }
        public string? RawBody { get; }


        public bool IsOk => Status == 200;
        public bool IsSuccessStatus => Status >= 200 && Status < 300;

        private EnableDebugModeResult(int status, string? rawBody) {
            Status = status;
            RawBody = rawBody;
        }

        internal static EnableDebugModeResult FromOk(string? rawBody = null) {
            return new EnableDebugModeResult(200, rawBody);
        }

        internal static EnableDebugModeResult FromOther(int status, string? rawBody = null) {
            return new EnableDebugModeResult(status, rawBody);
        }

        /// <summary>
        /// Exhaustive match across all declared response variants. The onOther handler covers any status not declared in the OpenAPI spec.
        /// </summary>
        public T Match<T>(Func<T> onOk, Func<int, string?, T> onOther)
        {
            if (Status == 200) return onOk();
            return onOther(Status, RawBody);
        }
    }
}
