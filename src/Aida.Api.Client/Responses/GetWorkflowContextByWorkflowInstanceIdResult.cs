// <auto-generated/>
#nullable enable
using System;
using System.IO;

namespace Aida.Api.Client.Responses {
    /// <summary>
    /// Discriminated response for GetWorkflowContextByWorkflowInstanceId. Exactly one of the typed properties corresponding to {Status} will be non-null; the others will be null. Use {Match} for exhaustive handling.
    /// </summary>
    public sealed class GetWorkflowContextByWorkflowInstanceIdResult {
        public int Status { get; }
        public string? RawBody { get; }


        public bool IsOk => Status == 200;
        public bool IsSuccessStatus => Status >= 200 && Status < 300;

        private GetWorkflowContextByWorkflowInstanceIdResult(int status, string? rawBody) {
            Status = status;
            RawBody = rawBody;
        }

        internal static GetWorkflowContextByWorkflowInstanceIdResult FromOk(string? rawBody = null) {
            return new GetWorkflowContextByWorkflowInstanceIdResult(200, rawBody);
        }

        internal static GetWorkflowContextByWorkflowInstanceIdResult FromOther(int status, string? rawBody = null) {
            return new GetWorkflowContextByWorkflowInstanceIdResult(status, rawBody);
        }

        /// <summary>
        /// Exhaustive match across all declared response variants. The onOther handler covers any status not declared in the OpenAPI spec.
        /// </summary>
        public T Match<T>(Func<T> onOk, Func<int, string?, T> onOther)
        {
            if (Status == 200) return onOk();
            return onOther(Status, RawBody);
        }
    }
}
