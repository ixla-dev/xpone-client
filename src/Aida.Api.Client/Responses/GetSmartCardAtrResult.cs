// <auto-generated/>
#nullable enable
using System;
using System.IO;

namespace Aida.Api.Client.Responses {
    /// <summary>
    /// Discriminated response for GetSmartCardAtr. Exactly one of the typed properties corresponding to {Status} will be non-null; the others will be null. Use {Match} for exhaustive handling.
    /// </summary>
    public sealed class GetSmartCardAtrResult {
        public int Status { get; }
        public string? RawBody { get; }

        public global::System.IO.Stream? Ok { get; }

        public bool IsOk => Status == 200;
        public bool IsSuccessStatus => Status >= 200 && Status < 300;

        private GetSmartCardAtrResult(int status, string? rawBody, global::System.IO.Stream? ok) {
            Status = status;
            RawBody = rawBody;
            Ok = ok;
        }

        internal static GetSmartCardAtrResult FromOk(global::System.IO.Stream? value, string? rawBody = null) {
            return new GetSmartCardAtrResult(200, rawBody, value);
        }

        internal static GetSmartCardAtrResult FromOther(int status, string? rawBody = null) {
            return new GetSmartCardAtrResult(status, rawBody, default);
        }

        /// <summary>
        /// Exhaustive match across all declared response variants. The onOther handler covers any status not declared in the OpenAPI spec.
        /// </summary>
        public T Match<T>(Func<global::System.IO.Stream, T> onOk, Func<int, string?, T> onOther)
        {
            if (Status == 200) return onOk(Ok!);
            return onOther(Status, RawBody);
        }
    }
}
