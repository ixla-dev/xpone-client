// <auto-generated/>
#nullable enable
using System;
using System.IO;

namespace Aida.Api.Client.Responses {
    /// <summary>
    /// Discriminated response for ReadFloat32Registers. Exactly one of the typed properties corresponding to {Status} will be non-null; the others will be null. Use {Match} for exhaustive handling.
    /// </summary>
    public sealed class ReadFloat32RegistersResult {
        public int Status { get; }
        public string? RawBody { get; }

        public float? Ok { get; }

        public bool IsOk => Status == 200;
        public bool IsSuccessStatus => Status >= 200 && Status < 300;

        private ReadFloat32RegistersResult(int status, string? rawBody, float? ok) {
            Status = status;
            RawBody = rawBody;
            Ok = ok;
        }

        internal static ReadFloat32RegistersResult FromOk(float? value, string? rawBody = null) {
            return new ReadFloat32RegistersResult(200, rawBody, value);
        }

        internal static ReadFloat32RegistersResult FromOther(int status, string? rawBody = null) {
            return new ReadFloat32RegistersResult(status, rawBody, default);
        }

        /// <summary>
        /// Exhaustive match across all declared response variants. The onOther handler covers any status not declared in the OpenAPI spec.
        /// </summary>
        public T Match<T>(Func<float, T> onOk, Func<int, string?, T> onOther)
        {
            if (Status == 200) return onOk(Ok!.Value);
            return onOther(Status, RawBody);
        }
    }
}
