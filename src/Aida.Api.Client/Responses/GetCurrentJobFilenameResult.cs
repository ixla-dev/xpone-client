// <auto-generated/>
#nullable enable
using System;
using System.IO;

namespace Aida.Api.Client.Responses {
    /// <summary>
    /// Discriminated response for GetCurrentJobFilename. Exactly one of the typed properties corresponding to {Status} will be non-null; the others will be null. Use {Match} for exhaustive handling.
    /// </summary>
    public sealed class GetCurrentJobFilenameResult {
        public int Status { get; }
        public string? RawBody { get; }

        public string? Ok { get; }

        public bool IsOk => Status == 200;
        public bool IsSuccessStatus => Status >= 200 && Status < 300;

        private GetCurrentJobFilenameResult(int status, string? rawBody, string? ok) {
            Status = status;
            RawBody = rawBody;
            Ok = ok;
        }

        internal static GetCurrentJobFilenameResult FromOk(string? value, string? rawBody = null) {
            return new GetCurrentJobFilenameResult(200, rawBody, value);
        }

        internal static GetCurrentJobFilenameResult FromOther(int status, string? rawBody = null) {
            return new GetCurrentJobFilenameResult(status, rawBody, default);
        }

        /// <summary>
        /// Exhaustive match across all declared response variants. The onOther handler covers any status not declared in the OpenAPI spec.
        /// </summary>
        public T Match<T>(Func<string, T> onOk, Func<int, string?, T> onOther)
        {
            if (Status == 200) return onOk(Ok!);
            return onOther(Status, RawBody);
        }
    }
}
